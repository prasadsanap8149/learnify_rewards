name: learnify_rewards
description: "Learn & Earn - Gamified learning platform with rewards for completing educational activities and quizzes."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.8.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Core dependencies for mobile app stores
  cupertino_icons: ^1.0.8

  # Firebase dependencies for backend services
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.3
  firebase_analytics: ^11.3.3
  firebase_crashlytics: ^4.1.3
  firebase_performance: ^0.10.0
  firebase_remote_config: ^5.1.3
  firebase_messaging: ^15.1.3

  # UI and Navigation
  go_router: ^14.6.2
  provider: ^6.1.2

  # Google Services
  google_sign_in: ^6.2.1
  google_mobile_ads: ^5.1.0

  # Additional mobile-focused packages
  shared_preferences: ^2.3.2
  package_info_plus: ^8.0.2
  device_info_plus: ^10.1.2
  connectivity_plus: ^6.0.5
  permission_handler: ^11.3.1
  local_auth: ^2.3.0
  biometric_storage: ^5.0.0

  # UI Components
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1
  image_picker: ^1.1.2

  # Utilities
  intl: ^0.19.0
  uuid: ^4.5.1
  crypto: ^3.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting and code quality
  flutter_lints: ^4.0.0

  # Testing utilities
  mockito: ^5.4.4
  build_runner: ^2.4.13
  fake_cloud_firestore: ^3.0.3

  # Build tools
  flutter_launcher_icons: ^0.14.1
  flutter_native_splash: ^2.4.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # App assets for mobile stores
  assets:
    - assets/images/
    - assets/icons/
    - assets/animations/
    - assets/sounds/

  # Custom fonts for better mobile experience
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Regular.ttf
        - asset: assets/fonts/Inter-Medium.ttf
          weight: 500
        - asset: assets/fonts/Inter-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Inter-Bold.ttf
          weight: 700

# Flutter Launcher Icons configuration
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/app_icon.png"
  min_sdk_android: 21
  web:
    generate: false
  windows:
    generate: false
  macos:
    generate: false
  linux:
    generate: false

# Flutter Native Splash configuration
flutter_native_splash:
  color: "#1E3A8A"
  image: assets/icons/splash_icon.png
  color_dark: "#0F172A"
  image_dark: assets/icons/splash_icon_dark.png
  android_12:
    image: assets/icons/splash_icon_android12.png
    icon_background_color: "#1E3A8A"
    image_dark: assets/icons/splash_icon_android12_dark.png
    icon_background_color_dark: "#0F172A"
  web: false
